// Contains some of the models that we will need for the DTO abstractions uses

// Defines the Message type
export type Message = {
    id: string,
    name: string,
    email: string,
    subject: string,
    message: string,
};